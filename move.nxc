task main(){
 // Aciona o robô para frente por 10s, depois o faz ir para tras
 // por 10s.
 OnFwd(OUT_A, 50);
 OnFwd(OUT_C, 50);
 Wait(10000);
 OnRev(OUT_A, 50);
 OnRev(OUT_C, 50);
 Wait(10000);
 Off(OUT_AC);
}
